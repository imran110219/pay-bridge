# ADR-002: Capability-based provider SPI

Use one small gateway port with a capabilities set and default unsupported operations. Separate interfaces were rejected initially because they complicate discovery without adding a needed runtime boundary.
