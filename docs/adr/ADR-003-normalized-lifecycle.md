# ADR-003: Normalized payment lifecycle

Expose deliberately coarse normalized states and retain safe provider state metadata. A false universal state machine was rejected because gateway semantics differ.
